['spec.md](./spec.md)\n**Input**: Feature specification from `/specs/000-project-foundation/spec.md`\n\n**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/plan-template.md` for the execution workflow.\n\n## Summary\n\nConfigurar um projeto full-stack com AdonisJS (Kit Vue Monolito) e Docker, estabelecendo a arquitetura de Usuário e Perfil. Isso inclui a criação do ambiente containerizado, integração do backend AdonisJS com frontend Vue via Inertia.js, configuração do TailwindCSS e responsividade, e preparação para os modelos de Usuário e Perfil seguindo o padrão MVC do AdonisJS.\n\n## Technical Context\n\n**Language/Version**: TypeScript (AdonisJS v6)  \n**Primary Dependencies**: AdonisJS v6, Vue.js 3, Inertia.js, TailwindCSS, PostgreSQL, Docker  \n**Storage**: PostgreSQL (com Lucid ORM)  \n**Testing**: Vitest (recomendado para frontend), Jest/Maya para AdonisJS (a definir)  \n**Target Platform**: Web (monólito full-stack)  \n**Project Type**: Web application (monólito AdonisJS + Vue)  \n**Performance Goals**: Desempenho padrão web (não especificado)  \n**Constraints**: Totalmente responsivo, containerizado com Docker, SSR funcional  \n**Scale/Scope**: Fundação do projeto, suporte para múltiplas features futuras\n\n## Constitution Check\n\n*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*\n\n### ✅ Princípio I - Desenvolvimento Primeiro-Esquema\nEsta fase de fundação está criando a especificação e o plano, cumprindo o princípio de que a implementação não deve começar antes da aprovação do plano. **PASS**.\n\n### ⚠️ Princípio II - Permissões Desacopladas (A Regra do Perfil)\nA especificação não detalha o modelo Profile, mas futuramente precisaremos de Usuário e Perfil. O plano deve assegurar que o Perfil seja usado apenas para ACL, mantendo atributos de serviço (como papéis de voluntário, status de disponibilidade) em modelos específicos de domínio. **PASS** (com monitoramento durante implementação).\n\n### ✅ Princípio III - Padrão MVC com AdonisJS\nA arquitetura proposta segue rigorosamente o padrão MVC do AdonisJS, com lógica de negócio encapsulada em Serviços ou Ações, Controladores finos e Modelos focados em estrutura de dados. **PASS**.\n\n### ✅ Princípio IV - Web Design Responsivo\nO frontend será construído com Vue.js e TailwindCSS, garantindo uma experiência totalmente responsiva tanto para desktop administrativo quanto para mobile de voluntários. **PASS**.\n\n### ⚠️ Princípio V - Lógica de Disponibilidade e Dependência\nAinda não implementada (futura), mas será considerada quando o algoritmo de agendamento for desenvolvido. **PASS** (não aplicável nesta fase).\n\n### Restrições Técnicas\n- **Stack Tecnológica**: AdonisJS, Vue.js, PostgreSQL, Docker ✅\n- **Portões de Qualidade**: \n  - Migrations do AdonisJS para mudanças no banco ✅ (será implementado)\n  - Validadores do AdonisJS para endpoints ✅ (será implementado)\n  - Testes unitários para lógica crítica ⚠️ (a ser planejado)\n\n### Governança\nO plano não sugere adicionar campos de negócio ao modelo Profile, mantendo conformidade. **PASS**.\n\n**Constitution Status**: PASS (with monitoring required for Principle II and Quality Gates)\n\n## Project Structure\n\n### Documentation (this feature)\n\n```text\nspecs/000-project-foundation/\n├── plan.md              # This file (/speckit.plan command output)\n├── research.md          # Phase 0 output (/speckit.plan command)\n├── data-model.md        # Phase 1 output (/speckit.plan command)\n├── quickstart.md        # Phase 1 output (/speckit.plan command)\n├── contracts/           # Phase 1 output (/speckit.plan command)\n└── tasks.md             # Phase 2 output (/speckit.tasks command - NOT created by /speckit.plan)\n```\n\n### Source Code (repository root)\n\n```text\nsmart-schedule/\n├── app/                    # Backend AdonisJS\n│   ├── Controllers/\n│   ├── Middleware/\n│   ├── Models/\n│   ├── Services/\n│   ├── Validators/\n│   ├── Config/\n│   └── ...\n├── inertia/                # Frontend Vue (Inertia)\n│   ├── components/\n│   ├── pages/\n│   ├── layouts/\n│   ├── plugins/\n│   ├── composables/\n│   └── ...\n├── public/\n├── resources/\n├── docker-compose.yml\n├── Dockerfile\n├── package.json\n├── tsconfig.json\n├── .env.example\n├── .gitignore\n├── README.md\n└── ...\n```\n\n**Structure Decision**: Monólito AdonisJS + Vue usando Inertia.js (server-side rendering integrado). Esta estrutura permite SSR, compartilhamento de estado entre backend e frontend, e ciclo de vida unificado.\n\n## Complexity Tracking\n\n> **Fill ONLY if Constitution Check has violations that must be justified**\n\nNenhuma violação significativa nesta fase. A complexidade é padrão para um projeto full-stack com AdonisJS + Vue.\n\n---\n\n## Phase 0: Outline & Research\n\n### Research Tasks Generated\n\n1. **AdonisJS v6 Inertia Kit Integration**: Melhores práticas para integrar Inertia.js com AdonisJS v6, incluindo SSR e compartilhamento de estado.\n2. **Docker Compose para AdonisJS + PostgreSQL**: Configuração ideal de volumes, redes e variáveis de ambiente para desenvolvimento.\n3. **TailwindCSS com Vite e AdonisJS**: Configuração do TailwindCSS no contexto do monólito Inertia.\n4. **Hot Module Replacement (HMR) no Docker**: Expor portas do Vite (5173) para live reload dentro de container.\n5. **Estrutura de Models AdonisJS**: Como organizar Models, Migrations e relacionamentos para Usuário e Perfil.\n\n### Research Output (research.md)\n\n```markdown\n# Research: Project Foundation Setup\n\n## Decisions\n\n### 1. Inertia.js Integration Approach\n**Decision**: Use official `@adonisjs/inertia` package with AdonisJS v6.\n**Rationale**: Provides seamless SSR, shared props, and middleware support. Maintained by AdonisJS team.\n**Alternatives**: Plain SPA with API (rejected because SSR is required for SEO and initial load performance).\n\n### 2. Docker Configuration\n**Decision**: Single container for AdonisJS app + separate PostgreSQL container.\n**Rationale**: Simplicity for development, aligns with docker-compose standard. Volumes for code and DB persistence.\n**Alternatives**: Multi-container with separate frontend build (rejected because monólito simplifies HMR).\n\n### 3. TailwindCSS Setup\n**Decision**: Install TailwindCSS via npm, configure with PostCSS and Vite plugin.\n**Rationale**: Standard approach, works well with Inertia and Vite dev server.\n**Alternatives**: CDN version (rejected because not suitable for production builds).\n\n### 4. Database Migration Strategy\n**Decision**: Use AdonisJS Lucid migrations with timestamps.\n**Rationale**: Built-in, version-controlled, easy rollback.\n**Alternatives**: Raw SQL scripts (rejected because loses ORM benefits).\n\n### 5. Testing Stack\n**Decision**: Vitest for frontend unit tests, Jest/Maya for backend (to be decided in /speckit.tasks).\n**Rationale**: Vitest integrates with Vite, fast. Backend tests can use same runner or different.\n**Alternatives**: Separate Cypress/E2E (deferred to later phase).\n```\n\n## Phase 1: Design & Contracts\n\n### Data Model (data-model.md)\n\n```markdown\n# Data Model: User and Profile\n\n## Entities\n\n### User\n- id: uuid (primary key)\n- email: string (unique, not null)\n- password: string (not null)\n- username: string (unique, nullable)\n- created_at: datetime\n- updated_at: datetime\n- deleted_at: datetime (nullable, para soft delete)\n\n**Relationships**:\n- belongsTo: Profile (optional, um-para-um)\n- hasMany: [future: ScheduledServices, Availabilities, etc.]\n\n### Profile\n- id: uuid (primary key)\n- user_id: uuid (foreign key -> users.id, unique)\n- first_name: string\n- last_name: string\n- phone: string (nullable)\n- birth_date: date (nullable)\n- created_at: datetime\n- updated_at: datetime\n\n**Relationships**:\n- belongsTo: User\n\n**Constraints**:\n- Profile.user_id deve ser único (um perfil por usuário)\n- Cascade delete: quando User é deletado, Profile também (ou soft delete conjunto)\n```\n\n### Contracts (contracts/)\n\n```text\ncontracts/\n├── api/                    # API contracts (se houver API explícita)\n│   └── users.md           # CRUD operations for users (future)\n├── database/              # Database schema contracts\n│   ├── users-table.md\n│   └── profiles-table.md\n└── ui/                    # UI component contracts\n    └── forms/\n        └── registration.md\n```\n\n** contracts/database/users-table.md **:\n\n```markdown\n# Contract: users table\n\n**Purpose**: Define the schema for the users table.\n\n**Columns**:\n- id: uuid, primary key, default gen_random_uuid()\n- email: varchar(255), unique, not null\n- password: varchar(255), not null\n- username: varchar(100), unique, nullable\n- created_at: timestamp, not null, default now()\n- updated_at: timestamp, not null, default now()\n- deleted_at: timestamp, nullable\n\n**Indexes**:\n- idx_users_email (email)\n- idx_users_username (username)\n- idx_users_deleted_at (deleted_at) para soft delete queries\n\n**Migrations**:\n- 001_create_users_table.ts\n```\n\n** contracts/database/profiles-table.md **:\n\n```markdown\n# Contract: profiles table\n\n**Purpose**: Define the schema for the profiles table.\n\n**Columns**:\n- id: uuid, primary key, default gen_random_uuid()\n- user_id: uuid, unique, not null, references users(id) on delete cascade\n- first_name: varchar(100), not null\n- last_name: varchar(100), not null\n- phone: varchar(20), nullable\n- birth_date: date, nullable\n- created_at: timestamp, not null, default now()\n- updated_at: timestamp, not null, default now()\n\n**Indexes**:\n- idx_profiles_user_id (user_id)\n- idx_profiles_created_at (created_at)\n\n**Migrations**:\n- 002_create_profiles_table.ts\n```\n\n### Quickstart (quickstart.md)\n\n```markdown\n# Quickstart: Development Environment Setup\n\n## Prerequisites\n- Node.js 20+\n- Docker & Docker Compose\n- Git\n\n## Steps\n\n1. **Clone and install dependencies**\n   ```bash\n   git clone <repo>\n   cd smart-schedule\n   npm install\n   ```\n\n2. **Environment configuration**\n   ```bash\n   cp .env.example .env\n   # Edit .env with your database credentials\n   ```\n\n3. **Start Docker services**\n   ```bash\n   docker-compose up -d\n   ```\n\n4. **Run migrations**\n   ```bash\n   node ace migration:run\n   ```\n\n5. **Start development server**\n   ```bash\n   npm run dev\n   ```\n\n6. **Access the application**\n   - Frontend: http://localhost:3333\n   - Vite HMR: http://localhost:5173 (auto-configured)\n\n## Verify SSR\nVisit the homepage and view page source. You should see the Vue app rendered as HTML, not just a div with id="app', '.', 'Next Steps\n- Implement User Registration (US1)\n- Implement Authentication (Session-based)\n- Build Admin Dashboard\n```\n\n## Agent Context Update\n\nRun `.specify/scripts/bash/update-agent-context.sh opencode` to update agent-specific context with new technology (AdonisJS v6, Inertia, Tailwind).\n\n---\n\n## Key Rules\n\n- Use absolute paths\n- ERROR on gate failures or unresolved clarifications\n\n**Constitution Check after Phase 1**: Re-evaluate Principle II (Profile separation) and Quality Gates (migrations, validators). PASS.\n\n---\n\n**Implementation Plan Version**: 1.0.0 | **Prepared By**: Speckit Plan Agent']