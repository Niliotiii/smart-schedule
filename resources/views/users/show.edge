<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Detalhes do Usuário</title>
  @vite(['resources/css/app.css', 'resources/js/app.js'])
</head>
<body class="bg-gray-50 text-gray-900 antialiased">
  @component('components/sidebar', { user, currentUrl: '/users' })
  @endcomponent

  <div class="md:ml-64 min-h-screen">
    @component('components/navbar', { user })
    @endcomponent

    <main class="p-4 md:p-6">
      <div class="flex items-center gap-2 mb-6">
        <a href="/users" class="text-gray-500 hover:text-gray-700">
          @component('components/icon', { name: 'chevron-left', cssClass: 'w-5 h-5' })
          @endcomponent
        </a>
        <h2 class="text-2xl font-bold text-gray-900">Detalhes do Usuário</h2>
      </div>

      <div class="bg-white rounded-lg shadow-sm border border-gray-200 p-6 max-w-2xl">
        <div class="space-y-4">
          <div>
            <label class="block text-sm font-medium text-gray-500">Nome</label>
            <p class="mt-1 text-sm text-gray-900">{{ userToShow.fullName || '-' }}</p>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-500">Email</label>
            <p class="mt-1 text-sm text-gray-900">{{ userToShow.email }}</p>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-500">Perfil</label>
            <p class="mt-1 text-sm text-gray-900">{{ userToShow.profile ? userToShow.profile.name : '-' }}</p>
          </div>
          <div>
            <label class="block text-sm font-medium text-gray-500">Criado em</label>
            <p class="mt-1 text-sm text-gray-900">{{ userToShow.createdAt.toFormat('dd/MM/yyyy HH:mm') }}</p>
          </div>
        </div>

        <div class="flex gap-2 mt-6 pt-4 border-t border-gray-200">
          @can('usersUpdate')
            <a href="/users/{{ userToShow.id }}/edit" class="inline-flex items-center gap-2 bg-violet-600 text-white px-4 py-2 rounded-md text-sm font-medium hover:bg-violet-700 transition-colors">
              @component('components/icon', { name: 'pencil', cssClass: 'w-4 h-4' })
              @endcomponent
              Editar
            </a>
          @endcan
          <a href="/users" class="inline-flex items-center gap-2 bg-white text-gray-700 px-4 py-2 rounded-md text-sm font-medium border border-gray-300 hover:bg-gray-50 transition-colors">
            Voltar
          </a>
        </div>
      </div>
    </main>
  </div>

  <script>
  function toggleSidebar() {
    const sidebar = document.getElementById('sidebar')
    const overlay = document.getElementById('sidebar-overlay')
    sidebar.classList.toggle('-translate-x-full')
    overlay.classList.toggle('hidden')
  }
  </script>
</body>
</html>