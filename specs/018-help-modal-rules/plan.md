# Implementation Plan: Help Modal with Rules

**Branch**: `018-help-modal-rules` | **Date**: 2026-05-18 | **Spec**: [spec.md](/specs/018-help-modal-rules/spec.md)
**Input**: Feature specification from `/specs/018-help-modal-rules/spec.md`

## Summary

Add a help button in the sidebar (above "Sair") that opens a modal with two tabs explaining: (1) auto-allocation rules, (2) schedule month statuses. Frontend-only, no backend changes.

## Technical Context

**Language/Version**: TypeScript, Vue 3, PrimeVue 4  
**Primary Dependencies**: PrimeVue (Dialog, TabView, TabPanel already available)  
**Storage**: N/A (static content)  
**Testing**: N/A (no new logic)  
**Target Platform**: Web (desktop + mobile)  
**Project Type**: Monolithic web app (Inertia Vue frontend)  
**Performance Goals**: Instant open (no data fetching)  
**Constraints**: No backend changes; content stored as static text in the component  

## Constitution Check

_GATE: Must pass before Phase 0 research. Re-check after Phase 1 design._

| Gate | Status | Notes |
|---|---|---|
| **III — Simplicidade** | ✅ Pass | Static informational content, zero backend complexity |
| **V — Privacidade** | ✅ Pass | No permissions needed — same info available to all authenticated users |

No violations. This is purely presentational.

## Project Structure

### Documentation (this feature)

```text
specs/018-help-modal-rules/
├── plan.md              # This file
├── research.md          # (empty — no unknowns)
├── data-model.md        # (empty — no new entities)
├── quickstart.md        # Integration scenarios
├── contracts/           # (empty — no new API contracts)
└── tasks.md             # Generated by /speckit.tasks
```

### Source Code (repository root)

```text
resources/js/Components/
├── AppMenu.vue          # + help button above Sair + modal integration
└── HelpModal.vue        # NEW: dialog with TabView, two tabs of static content
```

**Structure Decision**: Single AdonisJS project. No structural change. The help button and modal are new components within the existing component tree.

## Complexity Tracking

No constitution violations — no tracking needed.
